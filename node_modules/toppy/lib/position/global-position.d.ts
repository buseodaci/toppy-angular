import { InsidePlacement } from '../models';
import { ToppyPosition } from './position';
interface GlobalPositionConfig {
    placement?: InsidePlacement;
    offset?: number;
    width?: string | number;
    height?: string | number;
}
export declare class GlobalPosition extends ToppyPosition {
    protected config: GlobalPositionConfig;
    constructor(config: GlobalPositionConfig);
    getPositions(hostEl?: HTMLElement): any;
    private calc;
}
export {};
