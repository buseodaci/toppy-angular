export declare abstract class ToppyPosition {
    protected config: {};
    abstract getPositions(host: HTMLElement): any;
    getClassName(): string;
    updateConfig(config: any): void;
    init(tid: string): void;
}
